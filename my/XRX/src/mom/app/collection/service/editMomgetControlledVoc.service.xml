<xrx:service xmlns:eag="http://www.archivgut-online.de/eag" xmlns:xrx="http://www.monasterium.net/NS/xrx">
  <xrx:id>tag:www.monasterium.net,2011:/mom/service/editMomgetControlledVoc</xrx:id>
  <xrx:title>
    <xrx:i18n>
      <xrx:key></xrx:key>
      <xrx:default></xrx:default>
    </xrx:i18n>
  </xrx:title>
  <xrx:subtitle></xrx:subtitle>
  <xrx:description></xrx:description>
  <xrx:author>maburg</xrx:author>
  <xrx:licence>
This is a component file of the VdU Software for a Virtual Research Environment for the handling of Medieval charters.

As the source code is available here, it is somewhere between an alpha- and a beta-release, may be changed without any consideration of backward compatibility of other parts of the system, therefore, without any notice.

This file is part of the VdU Virtual Research Environment Toolkit (VdU/VRET).

The VdU/VRET is free software: you can redistribute it and/or modify
it under the terms of the GNU General Public License as published by
the Free Software Foundation, either version 3 of the License, or
(at your option) any later version.

VdU/VRET is distributed in the hope that it will be useful,
but WITHOUT ANY WARRANTY; without even the implied warranty of
MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
GNU General Public License for more details.

You should have received a copy of the GNU General Public License
along with VdU/VRET.  If not, see http://www.gnu.org/licenses.
  </xrx:licence>

 <xrx:variables>
   <xrx:variable>
      <xrx:name>$getindexName</xrx:name>
      <xrx:expression>request:get-parameter('indexname', '')</xrx:expression>    
    </xrx:variable>
    <xrx:variable>
      <xrx:name>$getlang</xrx:name>
      <xrx:expression>request:get-parameter('sprache', '')</xrx:expression>    
    </xrx:variable> 
     <!--   <xrx:variable>
      <xrx:name>$getsublemma</xrx:name>
      <xrx:expression>concat('#', request:get-parameter('sublemma', ''))</xrx:expression>    
    </xrx:variable>   -->       
  </xrx:variables>
<!-- das Service ist momentan noch so konstruiert, dass 3 Hierarchieebenen abgefragt werde können.
     durch den Wert der Variablen $getlemma wird die oberste hierachiebene bestimmt, wonach die
     children und grandchildren ausgelesen zusammengefügt und als json-objekt zurückgeliefert werden.
     Das json-objekt ($jsonO) sieht so aus: key = @rdf:about und value = prefLabel-Text auf Deutsch.
     Die @lang Einstellung ist derzeit noch hart gecodet, weil das widget diese info nicht bereitstellt.
     Wär schön, wenns gefixt wird.
  
 -->
  <xrx:init>
   <xrx:processor>
     <xrx:translateflag>false</xrx:translateflag>
   </xrx:processor>
  </xrx:init> 
  <xrx:body>
  { 
    let $vocabular := collection("/db/mom-data/metadata.controlledVocabulary.public")//atom:entry[contains(child::atom:id/text(), $getindexName)] 
    let $unbestimmteslabel := if ($getindexName = 'illurk-vocabulary') 
                              then ($vocabular//rdf:Description/skos:prefLabel[@xml:lang=$getlang] )
                              else($vocabular//rdf:RDF//skos:prefLabel ) 
     
     let $unbestimmteslemma := if ($getindexName = 'illurk-vocabulary')
                                then(data($unbestimmteslabel/parent::rdf:Description/@rdf:about) )
                                else(data($unbestimmteslabel/parent::skos:Concept/@rdf:about))     
     let $paare := (for $i in $unbestimmteslabel
             return  
             let $liste := if ($getindexName = 'illurk-vocabulary')
                           then(concat('"',normalize-space(replace(data($i/parent::rdf:Description/@rdf:about), '#', '')), '":"',normalize-space($i), '"'))
                           else(concat('"',normalize-space(replace(data($i/parent::skos:Concept/@rdf:about), '#', '')), '":"',normalize-space($i), '"'))
                    
             return  $liste)
      let $jsonO := jsonx:object($paare)
      return $jsonO          
    
     }
  </xrx:body>
</xrx:service>
